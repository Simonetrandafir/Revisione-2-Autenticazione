import { CapitalizzaPipe } from './capitalizza.pipe';

describe('CapitalizzaPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizzaPipe();
    expect(pipe).toBeTruthy();
  });
});
