import { ConvertiErrorePipe } from './converti-errore.pipe';

describe('ConvertiErrorePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertiErrorePipe();
    expect(pipe).toBeTruthy();
  });
});
